import * as actions from '../actionTypes';

const initialState = [];

export default function ${reducerName}Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
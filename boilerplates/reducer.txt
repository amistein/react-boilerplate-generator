import * as actions from '../actionTypes';

const initialState = [];

export default function REDUCER_NAMEReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}